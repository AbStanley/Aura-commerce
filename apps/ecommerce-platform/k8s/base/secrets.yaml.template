# IMPORTANT: This is a TEMPLATE. Replace placeholder values before applying.
# In production, use sealed-secrets, external-secrets, or similar.
apiVersion: v1
kind: Secret
metadata:
  name: ecommerce-secrets
  namespace: ecommerce
type: Opaque
stringData:
  # Database
  POSTGRES_USER: "admin"
  POSTGRES_PASSWORD: "<REPLACE_WITH_STRONG_PASSWORD>"
  
  # RabbitMQ
  RABBITMQ_DEFAULT_USER: "guest"
  RABBITMQ_DEFAULT_PASS: "<REPLACE_WITH_STRONG_PASSWORD>"
  
  # JWT
  JWT_SECRET: "<REPLACE_WITH_32_CHAR_SECRET>"
  
  # Seq Admin Password
  SEQ_ADMIN_PASSWORD: "<REPLACE_WITH_ADMIN_PASSWORD>"
